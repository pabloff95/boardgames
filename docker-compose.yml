services:
  django:
    build:
      target: dev
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    env_file:
    - backend/.env
    volumes:
      - ./backend:/app 

volumes:
  postgres_data: